# Plugins this Jenkinsfile needs, installed with `jenkins-plugin-cli`
# (bundled in the official jenkins/jenkins:lts image) against a full Jenkins
# controller - see the "How this was verified" section in README.md.
#
# Core pipeline syntax (declarative pipeline, sh/dir/checkout/archiveArtifacts)
# ships with Jenkins itself and every officially supported plugin below pulls
# in its own transitive dependencies automatically, so this list only needs
# to name what the Jenkinsfile calls directly:
#   - workflow-aggregator: declarative Pipeline plugin suite
#   - git: SCM checkout (the Checkout stage, and this job's own definition)
#   - junit: the `junit` step used in the Unit Tests stage
#   - htmlpublisher: the `publishHTML` step used in the Publish Report stage
#   - ws-cleanup: the `cleanWs()` step used in the post/cleanup block
#   - timestamper: the `timestamps()` pipeline option

workflow-aggregator
git
junit
htmlpublisher
ws-cleanup
timestamper
